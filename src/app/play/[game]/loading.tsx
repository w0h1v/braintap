import { PlaySkeleton } from "@/components/play/PlaySkeleton";

/** Route-level loading UI shown while the game segment hydrates. */
export default function PlayLoading() {
  return <PlaySkeleton />;
}
